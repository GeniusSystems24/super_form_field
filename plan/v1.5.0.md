# Super Form Field — Version 1.5.0

Review the package and complete the following tasks while preserving the existing architecture, API compatibility where practical, and the overall design language of the library.

## Task 1 — Unify InputDecoration across all form fields

Refactor all form field widgets to accept a single `InputDecoration` instead of passing individual properties such as `label`, `hint`, and similar parameters.

Apply this change consistently to:

- `lib/src/features/super_attachment_form_field`
- `lib/src/features/super_bool_form_field`
- `lib/src/features/super_choice_form_field`
- `lib/src/features/super_date_form_field`
- `lib/src/features/super_multi_select_form_field`
- `lib/src/features/super_numeric_form_field`
- `lib/src/features/super_select_form_field`
- `lib/src/features/super_text_form_field`

### Requirements

- Preserve the current visual style and behavior of the library.
- Continue applying the package's custom design system to the provided `InputDecoration`.
- Maintain backward compatibility whenever practical.
- Eliminate duplicated decoration-related APIs and use `InputDecoration` as the single source of truth.

---

## Task 2 — Improve field interactions

### `super_date_form_field`

Improve the mobile interaction:

- Tapping the calendar icon should dismiss the keyboard.
- Display the date picker inside a Bottom Sheet.
- Keep the desktop and tablet experience unchanged unless necessary.

### `super_numeric_form_field`

Fix the widget layout:

- Vertically center the input text within the field.
- Make the increment and decrement buttons stretch to match the full height of the input field.
- Ensure the layout remains fully consistent with the library's design system.

---

## Task 3 — Update the example project

In:

`example/lib/demos/demo_scaffold.dart`

- Remove the existing `SectionCard`.
- Replace it with equivalent components from `super_core`.
- Preserve the current functionality while matching the library's design system.

---

## Documentation

Update all project documentation and metadata.

### README

- Rewrite `README.md` following **pub.dev** documentation style and best practices.
- Ensure all public APIs and new features are properly documented.
- Update usage examples to reflect the latest API changes.

### CHANGELOG

- Update `CHANGELOG.md` with all changes introduced in this release.

### pubspec

Update `pubspec.yaml`:

- Set the package version to **1.3.0**.
- Ensure the package description does not exceed **512 characters**.
- Update any metadata affected by the changes.

### AI Skills

- Update all AI skills to reflect the new APIs and architecture.
- Ensure every skill is synchronized with the latest package behavior and examples.

### Examples

- Update all example screens to use the new APIs.
- Ensure every example compiles successfully and demonstrates the latest functionality and best practices.

---

## Deliverables

Provide:

- A ZIP file containing the updated package.
- A Git patch (`.patch`) file containing all changes.

---

## Task 4 — Fix `super_numeric_form_field` layout issues

The `lib/src/features/super_numeric_form_field` input text alignment issue still exists. The previous fix only moved the text from the bottom to the top, but it must be vertically centered inside the input field. Review the attached screenshot carefully and fix the layout at its root cause rather than using visual offsets or hard-coded padding.

Also update the increment and decrement buttons so that:

- Each button has a square size where `width` equals `height`.
- The button size matches the full height of the input field.
- There is no gap between the increment and decrement buttons.
- The buttons remain properly aligned with the field.
- The result stays responsive and consistent with the GeniusLink Design System across supported screen sizes and themes.
