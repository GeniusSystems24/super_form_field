/// Controls where a field surfaces validation feedback.
enum ValidationPosition {
  /// Shows the validation icon in the input/control suffix.
  suffixIcon,

  /// Shows validation text under the control.
  underBox,

  /// Shows the validation icon at the trailing edge of the label row.
  labelTrailing,
}
